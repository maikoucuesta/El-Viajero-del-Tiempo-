import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miguel.elviajero',
  appName: 'El Viajero Del Tiempo',
  webDir: 'www'
};

export default config;
